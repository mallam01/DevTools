
###### GENERAL COMMANDS #####
git init   // initiate git on your local 
git add .  // add all the files to the branch
git commit -m "MESSAGE"  // commit all the changes to the master. 
git push // push the changes from your local to the repositry. publish local commands 
git status // check status 
git branch -av // show all the local and remote branches 
git checkout BRANCH_NAME // point to certian branch 

###### MERGE WITH THE MASTER #####
git checkout master
git pull
git status // no changes locally 
checkout BRANCH_NAME
git merge master 
>>> modify the changes >> tool > comapre
git add .
git commit -m ""
git push

###### REVERT CHANGES #####
git reset --hard 
git reset --hard head~;
git merge --abort 
git clean fd 
git diff 

###### CHECKOUT BRANCH/FILES #####
git checkout BRANCH_NAME // point to certian branch 
git checkout FILE_PATH_ON_BITBUCKET

###### IGNORE/SKIP FILE #####
git update-index --assume-unchanged <FILE_NAME>
